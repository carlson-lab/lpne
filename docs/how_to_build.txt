To make the docs locally:

$ cd path/to/lpne/docs/
$ make html

Then open build/html/index.html.

To edit the docs, change the *.rst files in source or add more *.rst files
and mention them in source/index.rst.
